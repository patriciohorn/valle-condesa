---

const {prefix, number, suffix, label} = Astro.props
---

<li class="p-8 flex flex-col items-center justify-center gap-y-2 bg-white shadow-sm rounded-2xl">
  <div class="flex items-center gap-x-1">
    {prefix && <span class="text-4xl text-heading">{prefix}</span>}
    <span class="text-6xl text-heading">{number}</span>
    {suffix && <span class="text-4xl self-end text-heading ">{suffix}</span>}
  </div>
  <p class="text-xl text-slate-600">{label}</p>
</li>