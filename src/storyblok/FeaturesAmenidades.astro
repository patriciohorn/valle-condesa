---
import SectionWrapper from "../components/SectionWrapper.astro";
import AmenityCard from "./AmenityCard.astro";

const {blok} = Astro.props
---


<SectionWrapper>
  <div class="pb-28 grid md:grid-cols-4 gap-x-8 gap-y-12">
     {blok.amenidades?.map((card : any) => (
      <AmenityCard card={card}/>
  ))}
  </div>
 
</SectionWrapper>